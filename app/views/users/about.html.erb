<% provide(:titulo, "#{@seluser.username} About me") %>

<% if @seluser.detail %>
    <%= render 'details/about', obj: @seluser.detail %>
<% else %>
     <div class="well cv col-xs-8 col-offset-2">
        <div class='col-xs-2 well-logo'>
        </div>
        <div class='col-xs-12'>
            <h2 align='center'> About me</h2>
        </div>
    </div>
<% end %>